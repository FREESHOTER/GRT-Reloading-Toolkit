# build output
bin/
obj/
artifacts/
dist/
*.user
*.suo
.vs/

# make-icons.ps1's old output folder — it now writes into GrtReloadingToolkit/plugin/media/icons/,
# which is committed. Kept so a stale local copy doesn't show up as untracked.
grt-plugins-shared/icons/

# local
*.zip
